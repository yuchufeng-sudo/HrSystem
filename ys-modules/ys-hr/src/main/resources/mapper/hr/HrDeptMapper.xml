<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ys.hr.mapper.HrDeptMapper">

    <resultMap type="HrDept" id="HrDeptResult">
        <result property="deptId"    column="dept_id"    />
        <result property="deptName"    column="dept_name"    />
        <result property="orderNum"    column="order_num"    />
        <result property="leader"    column="leader"    />
        <result property="status"    column="status"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="enterpriseId"    column="enterprise_id"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <resultMap type="com.ys.hr.domain.vo.DepartmentsVo" id="DepartmentsResult">
        <result property="name"    column="dept_name"    />
        <result property="value"    column="employee_count"    />
    </resultMap>

    <sql id="selectHrDeptVo">
        select dept_id, dept_name, order_num, leader, status, del_flag, create_by, create_time, update_by, update_time, remark, enterprise_id from hr_dept
    </sql>

    <select id="selectHrDeptList" parameterType="HrDept" resultMap="HrDeptResult">
        select d.dept_id,d.enterprise_id, d.dept_name, d.order_num, d.leader,
               d.status, d.del_flag, d.create_by, d.create_time,d.remark
            ,(select string_agg(avatar_url,',') from (select avatar_url from hr_employees u where u.employee_id
            in (select employee_id from hr_dept_employees de where d.dept_id = de.dept_id) limit 3) a) avatars
            ,(select count(1) from hr_dept_employees de where de.dept_id = d.dept_id) empNum
            ,u.full_name leaderName
        from hr_dept d
            left join hr_employees u on u.employee_id = d.leader
        <where>
            <if test="leaderName != null  and leaderName != ''"> and u.full_name ILIKE concat('%', #{leaderName}, '%')</if>
            <if test="deptName != null  and deptName != ''"> and d.dept_name ILIKE concat('%', #{deptName}, '%')</if>
            <if test="orderNum != null  and orderNum != ''"> and d.order_num = #{orderNum}</if>
            <if test="leader != null  and leader != ''"> and d.leader = #{leader}</if>
            <if test="status != null  and status != ''"> and d.status = #{status}</if>
            <if test="enterpriseId != null  and enterpriseId != ''"> and d.enterprise_id = #{enterpriseId}</if>
        </where>
    </select>

    <select id="selectHrDeptByDeptId" parameterType="Long" resultMap="HrDeptResult">
        select dept_id, dept_name, order_num, leader, status, del_flag, create_by, create_time, update_by, update_time, enterprise_id, remark
        from hr_dept
        where dept_id = #{deptId}
    </select>

    <delete id="deleteHrDeptEmployeesByDeptIds" parameterType="Long">
        delete from hr_dept_employees where dept_id in
        <foreach item="deptId" collection="array" open="(" separator="," close=")">
            #{deptId}
        </foreach>
    </delete>

    <delete id="deleteHrDeptEmployeesByDeptId" parameterType="Long">
        delete from hr_dept_employees where dept_id = #{deptId}
    </delete>

    <delete id="deleteHrDeptEmployeesByEmployeesId" parameterType="Long">
        delete from hr_dept_employees where employee_id = #{employeeId}
    </delete>

    <insert id="batchHrDeptEmployees">
        insert into hr_dept_employees(dept_id, employee_id) values
        <foreach item="item" index="index" collection="list" separator=",">
            (#{item.deptId}, #{item.employeeId})
        </foreach>
    </insert>

    <select id="getDeptList" parameterType="com.ys.hr.domain.HrDept" resultMap="HrDeptResult">
        SELECT
        d.dept_id,
        d.enterprise_id,
        d.dept_name,
        d.leader,
        d.status,
        d.del_flag,
        d.create_by,
        d.create_time,
        -- 取出除自己之外的前三个成员头像
        (select string_agg(avatar_url,',') from (select avatar_url from hr_employees u where u.employee_id
                                                                                                 in (select employee_id from hr_dept_employees de where d.dept_id = de.dept_id) limit 3) a) avatars
        FROM hr_dept d
        WHERE d.del_flag = '0'
        AND d.dept_id IN (
        SELECT dept_id
        FROM hr_dept_employees
        WHERE employee_id = #{empId}
        )
    </select>

    <select id="selectDepartments" parameterType="String" resultMap="DepartmentsResult">
        WITH emp_counts AS (
            SELECT
                dept_id,
                COUNT(*) AS cnt
            FROM
                hr_dept_employees
            GROUP BY
                dept_id
        )
        SELECT
            d.dept_name,
            COALESCE(ec.cnt, 0) AS employee_count
        FROM
            hr_dept d
                LEFT JOIN emp_counts ec
                          ON d.dept_id = ec.dept_id
        WHERE
            d.enterprise_id = #{userEnterpriseId}
        ORDER BY employee_count DESC
    </select>
    <select id="selectHrDeptEmployeesCountByEmployeeIds" resultType="String">
        select full_name from hr_dept_employees a
            left join hr_employees b on a.employee_id = b.employee_id
            where a.employee_id in
        <foreach item="employeeId" collection="array" open="(" separator="," close=")">
            #{employeeId}
        </foreach>
    </select>
</mapper>
